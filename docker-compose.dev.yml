name: bylucky-dev

services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: bylucky
      POSTGRES_USER: bylucky
      POSTGRES_PASSWORD: bylucky_dev
      TZ: Asia/Shanghai
    # Keep PostgreSQL private to the host. The application connects through 127.0.0.1.
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5431}:5432"
    volumes:
      - bylucky_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bylucky -d bylucky"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

volumes:
  bylucky_postgres_data:
